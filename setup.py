"""
Legacy setup.py for backward compatibility.

This is kept for backward compatibility but the main configuration is in pyproject.toml.
"""

from setuptools import setup

if __name__ == "__main__":
    setup()
